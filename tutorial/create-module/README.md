[Create Module](https://go.dev/doc/tutorial/create-module)

The library module lives in `tutorial\create-module\greetings`.

From the repo root, run its consumer with:

```powershell
go run .\tutorial\call-module-code
```
