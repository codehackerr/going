[Create Module](https://go.dev/doc/tutorial/create-module)

The library module lives in `.\greetings`.

From this directory, run its consumer with:

```powershell
..\go-local.cmd run .\call-module-code
```
